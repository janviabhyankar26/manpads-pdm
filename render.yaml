services:
  - type: web
    name: manpads-pdm-api
    runtime: python
    buildCommand: pip install -r backend/requirements.txt -r requirements.txt
    startCommand: PYTHONPATH=. uvicorn backend.main:app --host 0.0.0.0 --port $PORT
